package net.cyberflame.lumberharvester.commands;
/*
import net.cyberflame.lumberharvester.Main;
import org.bukkit.ChatColor;
import org.bukkit.command.Command;
import org.bukkit.command.CommandExecutor;
import org.bukkit.command.CommandSender;
*/

/*
public class RootCommand implements CommandExecutor {
    @Override
    public boolean onCommand(CommandSender sender, Command command, String label, String[] args) {
        if (args.length == 0) {
            sender.sendMessage(ChatColor.GOLD + "LumberHarvester v" + Main.getInstance().getDescription().getVersion());
            sender.sendMessage(ChatColor.GOLD + "Created by CyberFlame");
            sender.sendMessage(ChatColor.GOLD + "https://cyberflame.net");
            sender.sendMessage(ChatColor.GOLD + "

*/
@SuppressWarnings("CommentedOutCode")
public class RootCommand // implements CommandExecutor
{
    /*
    @Override
    public boolean onCommand(CommandSender sender, Command command, String label, String[] args)
    {
        final String message = Main.getInstance().getConfig().getString("messages.from-command");
        sender.sendMessage(ChatColor.translateAlternateColorCodes('&', message));
        return true;
    }
    */
}